% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_csmart_data.R
\name{simulate_csmart_data}
\alias{simulate_csmart_data}
\title{Simulate a toy two-stage cSMART dataset}
\usage{
simulate_csmart_data(
  n_clusters = 24,
  cluster_size = 12,
  rerandomization = c("nonresponder", "responder", "full"),
  p_rerand = 0.6,
  icc = 0.1,
  seed = NULL
)
}
\arguments{
\item{n_clusters}{Number of clusters.}

\item{cluster_size}{Either a single common cluster size or an integer vector
of length \code{n_clusters}.}

\item{rerandomization}{Either \code{"full"} for type I cSMART-style data, where
all clusters are re-randomized at stage 2, \code{"nonresponder"} for a
limited-re-randomization setting in which non-responder clusters are
re-randomized, or \code{"responder"} for a limited-re-randomization setting in
which responder clusters are re-randomized.}

\item{p_rerand}{Approximate proportion of clusters re-randomized at stage 2
when \code{rerandomization} is \code{"nonresponder"} or \code{"responder"}.}

\item{icc}{Approximate intra-cluster correlation for the individual-level
outcome.}

\item{seed}{Optional random seed.}
}
\value{
A data frame with one row per individual.
}
\description{
Generates a simple individual-level cSMART dataset for examples and package
documentation. The returned data are in the format expected by \code{\link[=cQL]{cQL()}}:
one row per individual, a cluster identifier, separate stage-1 and stage-2
treatment columns, and \code{NA} in the stage-2 treatment column for clusters not
re-randomized at stage 2.
}
\examples{
toy_data <- simulate_csmart_data(seed = 11)
head(toy_data)
}
