% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cQL-package.R, R/cQL.R
\docType{package}
\name{cQL}
\alias{cQL-package}
\alias{cQL}
\title{cQL: Clustered Q-Learning for cSMART Data}
\usage{
cQL(
  data,
  stage2_formula,
  stage1_formula,
  cluster,
  stage1_treat,
  stage2_treat,
  stage2_tailoring_vars,
  alpha = 0.05,
  n_boot = 1000,
  working_correlation = c("exchangeable", "independence"),
  m_method = c("fixed_xi", "full_n", "user"),
  fixed_xi = 0.025,
  m = NULL,
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing one row per individual.}

\item{stage2_formula}{A formula for the stage-2 Q-function, for example
\code{Y ~ X1 * A1 + A1 * A2 + A2:X2}.}

\item{stage1_formula}{A formula for the stage-1 Q-function, for example
\code{Y1 ~ X1 * A1}. The response name does not need to exist in \code{data}; it is
created internally as the stage-1 pseudo-outcome.}

\item{cluster}{Character scalar giving the cluster identifier column name.}

\item{stage1_treat}{Character scalar giving the stage-1 treatment column
name.}

\item{stage2_treat}{Character scalar giving the stage-2 treatment column
name. For limited second-stage re-randomization, clusters not
re-randomized at stage 2 should have \code{NA} in this column.}

\item{stage2_tailoring_vars}{Character vector of cluster-level candidate
tailoring variables that interact with \code{stage2_treat} in
\code{stage2_formula}. For the example \code{Y ~ X1 * A1 + A1 * A2 + A2:X2}, use
\code{c("A1", "X2")}.}

\item{alpha}{Significance level used for the bootstrap confidence intervals.
Defaults to \code{0.05} for 95\% confidence intervals.}

\item{n_boot}{Number of bootstrap replicates for each stage. Defaults to
\code{1000}.}

\item{working_correlation}{Working correlation used when estimating the
non-regularity measure that determines the stage-1 resample size.
\code{"exchangeable"} is the manuscript default.}

\item{m_method}{Method for selecting the stage-1 cluster resample size.
\code{"fixed_xi"} uses the manuscript's fixed tuning parameter rule,
\code{"full_n"} uses the full-cluster bootstrap with \code{M = N}, and \code{"user"}
uses the value supplied through \code{m}.}

\item{fixed_xi}{Tuning parameter used when \code{m_method = "fixed_xi"}. The
manuscript default is \code{0.025}.}

\item{m}{User-supplied stage-1 cluster resample size. Required when
\code{m_method = "user"}.}

\item{seed}{Optional random seed.}

\item{verbose}{Logical; if \code{TRUE}, prints the selected stage-1 resample size
and the bootstrap progress warnings when relevant.}
}
\value{
An object of class \code{"cQL_fit"} containing:
\itemize{
\item \code{stage_summary}: a data frame reporting each stage's total number of
clusters \code{N}, number of randomized clusters \code{N_rand}, and bootstrap
resample size \code{M};
\item \code{stage2}: a data frame with the stage-2 coefficient estimates,
bootstrap standard errors, confidence intervals, p-values, and
significance stars;
\item \code{stage1}: the analogous stage-1 output;
\item \code{p_hat}: the estimated degree of non-regularity;
\item \code{stage1_m}: the selected stage-1 cluster resample size;
\item \code{stage2_cluster_count}: the number of clusters re-randomized at stage 2;
\item \code{cluster_count}: the total number of clusters;
\item \code{design}: \code{"full_second_stage_rerandomization"} or
\code{"limited_second_stage_rerandomization"};
\item \code{treatment_mapping}: the internal \code{-1/+1} recoding used for the two
treatment columns.
}
}
\description{
\code{cQL} implements the manuscript's clustered Q-learning workflow for
two-stage clustered SMART data using the M-out-of-N cluster bootstrap.
The main entry point is \code{\link[=cQL]{cQL()}}, which fits the stage-2 and stage-1
Q-functions on one user-supplied cSMART dataset and returns bootstrap-based
confidence intervals and p-values for the regression coefficients.

Fits a two-stage clustered Q-learning analysis for one cSMART dataset using
the M-out-of-N cluster bootstrap described in the manuscript. The function
supports both:
}
\details{
\itemize{
\item type I cSMARTs, where every cluster is re-randomized at stage 2; and
\item type II/III cSMARTs, where only a subset of clusters is re-randomized at
stage 2 and the stage-2 treatment column is \code{NA} for clusters not
re-randomized.
}

The data must be at the individual level, with one row per individual.
Cluster-level treatments and stage-2 tailoring variables should be repeated
across individuals within each cluster.
}
\examples{
toy_data <- simulate_csmart_data(
  n_clusters = 48,
  cluster_size = 12,
  rerandomization = "nonresponder",
  p_rerand = 0.7,
  seed = 7
)

fit <- cQL(
  data = toy_data,
  stage2_formula = Y ~ X1 * A1 + A1 * A2 + A2:X2,
  stage1_formula = Y1 ~ X1 * A1,
  cluster = "cluster_id",
  stage1_treat = "A1",
  stage2_treat = "A2",
  stage2_tailoring_vars = c("A1", "X2"),
  n_boot = 200,
  seed = 1,
  verbose = FALSE
)

fit$stage_summary
fit
}
\author{
\strong{Maintainer}: Yao Song \email{yaos@umich.edu}

}
